{
  "name": "@ai-nft-forge/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ops:capture": "node dist/index.js capture-observability",
    "start": "node dist/index.js start",
    "health": "node dist/index.js health",
    "build": "rm -rf dist tsconfig.tsbuildinfo && tsc -p tsconfig.json",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@ai-nft-forge/database": "workspace:*",
    "@ai-nft-forge/shared": "workspace:*",
    "bullmq": "^5.73.0",
    "dotenv": "^17.4.0",
    "ioredis": "^5.10.1"
  }
}
